export interface Theme {
  font: {
    [key: string]: string;
  };
  colours: {
    [key: string]: string;
  };
  border: {
    [key: string]: string;
  };
  layout: {
    [key: string]: string;
  };
}
